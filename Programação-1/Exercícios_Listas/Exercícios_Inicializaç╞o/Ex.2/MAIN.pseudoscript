array = []
condicional = Verdadeiro

enquanto condicional:
    entrada('Pressione 1 para adicionar um número ou 2 para sair.', x)
    se x == 1:
        entrada('Qual o número que deseja adiconar?', y)
        array.adiciona(y)
    senao x == 2:
        condicional = Falso

print(array)