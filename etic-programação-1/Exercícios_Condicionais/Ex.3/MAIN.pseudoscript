
funcao anoBissexto(ano):
    se ano % 4 == 0 e ano % 100 != 0:
        retorna True

entrada("Diga o ano: ", x)

se anoBissexto(x):
    imprime(f"O ano {x} é bissexto")
senao:
    imprime(f"O ano {x} não é bissexto")