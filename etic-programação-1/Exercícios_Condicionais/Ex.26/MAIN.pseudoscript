
funcao eh_quadrado_perfeito(numero):
    para cada elemento em(1, numero + 1):
        se elemento ** 2 == numero:
        retorna Verdadeiro
    retorna Falso 

entrada("Indique um número para saber se é um quadrado perfeito.", numero)

se eh_quadrado_perfeito(numero):
    imprime("O número é um quadrado perfeito")
senao:
    imprime("O número não é um quadrado perfeito")
