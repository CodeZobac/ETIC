modelo Verificador:
    funcao ano_primo(ano):
        para num em (2, ano - 1):
            se ano % num != 0:
                imprime("O ano é primo")
            senao:
                imprime("O ano não é primo")

entrada("Diga um ano para saber se é um ano primo ou não: ", ano)

Verificador().ano_primo(ano)
